{
  "cli": {
    "version": ">= 12.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "//": "These profiles support the optional `eas build --local` path (e.g. `eas build --local -p android --profile preview`). The committed GitHub Actions workflows use bare prebuild + Gradle/xcodebuild instead, so no Expo account is required.",
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      },
      "ios": {
        "simulator": false
      }
    },
    "production": {
      "autoIncrement": true,
      "android": {
        "buildType": "app-bundle"
      },
      "ios": {}
    }
  },
  "submit": {
    "production": {}
  }
}
